<svg class="progress" version="1.1" xmlns="http://www.w3.org/2000/svg"
     width={{size}}
     height={{size}}
     viewBox={{_viewBox}}>
  <circle fill="transparent"
          cx={{_center}}
          cy={{_center}}
          r={{_radius}}
          stroke-dasharray={{_circumference}}
          style={{{_dashOffsetStyle}}}
          stroke-width={{strokeWidth}}
          transform={{_transform}}>
  </circle>
</svg>
